import React, { useContext } from 'react';
import { CallContext } from '../Contexting/Context';

const Scroll = () => {
    return (
        <div>
            scrooll
        </div>
    );
};

export default Scroll;